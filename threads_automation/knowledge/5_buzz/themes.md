# 5_buzz テーマ候補（需要の答え合わせ）

> `python -m threads_automation.buzz themes` が自動生成・上書きするファイル。
> バズ投稿（＝世の中の需要の答え）から抽出した「今伸びるテーマ」を並べる。
> writer / autopost はこのテーマ群を優先して投稿ネタに使う（＝外さないための起点）。
> 手で編集せず、バズ収集 → 再抽出で更新する。

---

（まだ抽出されていません。`data/buzz/` にバズ投稿を貯めてから
`python -m threads_automation.buzz themes` を実行すると、
表示数の裏付けが強い順に「今伸びるテーマ Top10」がここに書き出されます。）
